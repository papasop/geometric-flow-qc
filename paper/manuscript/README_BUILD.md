# Manuscript Artifact

The authoritative manuscript artifact for revision `v0.9.4-m3` is:

```text
shor-exact-response-fibre-v0.9.4-manuscript-m3.pdf
```

The reconstructed corrective/review manuscript artifact for revision
`v0.9.4-m4` is:

```text
shor-exact-response-fibre-v0.9.4-manuscript-m4-reconstructed.pdf
```

The computational protocol remains `v0.9.4`. This manuscript-only revision
changes exposition, methodological disclosure, statistical interpretation,
literature positioning, and presentation only. It does not modify or rerun the
frozen computation.

The m4 PDF was externally compiled from reconstructed source under
`docs/manuscript/reconstructed-v0.9.4-m4/`; the external audit reports
5 pages, errors 0, undefined references/citations 0, overfull boxes 0, and no
visual clipping, overlap, broken tables, or visible hyperlink borders.

The archived m3 PDF has 9 pages and 14 references. The reconstructed m4 PDF has
5 pages and 8 references, so it must not be represented as a complete
source-equivalent reproduction of m3 or as a final submission replacement
without further authorization.
